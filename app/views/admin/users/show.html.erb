
<div class='col-lg-12'>
  <div class='text-center'>
    <h1>マイページ</h1>
  </div>
</div>
<div class='col-lg-5 col-lg-offset-1'>
  <h2>ユーザー情報</h2>
  <div class='form-group'>
    <label>ユーザ画像</label>
    <br>
  </div>
  <br>
  <div class='form-group'>
    <label>ユーザー名</label>
    <br>
  </div>
  <br>
  <div class='form-group'>
    <label>好きなチーム名</label>
    <br>
    <br>
  </div>
  <br>
  <div class='form-group'>
    <label>自己紹介</label>
    <br>
  </div>
  <br>
</div>
<div class='col-lg-6'>
  <h2>ユーザー投稿一覧</h2>
  <table class='table table-hover'>
    <thead>
      <tr>
        <th>球場画像</th>
        <th>投稿者</th>
        <th>球場名</th>
        <th>観戦日</th>
        <th>投稿詳細</th>
      </tr>
    </thead>
  </table>
</div>
<tbody>
      <% @posts.each do |post| %>
        <tr>
          <td><%= attachment_image_tag post, :posted_image, :fill, 250, 250, fallback: "no_image.png", size:'50x50' %>
          <td><%= post.stadium.name %></td>
          <td>
            <%= link_to post_path(post.id) do %>
              <%= post.user.name %>
          <td><%= post.watching_date.strftime("%Y年%m月%d日")  %>
          <td><%= post.post_content %>
          <% if user_signed_in? %>
           <% if post.favorited_by?(current_user) %>
             <!--  ログインしているユーザーがファボしたかどうかで分岐 -->
              <td>
                  <span>いいね</span>
                  <%= link_to post.favorites.count, post_favorites_path(post.id), method: :delete %>
                  </td>
            <% else %>
              <td>
                  <span>いいね</span>
                  <%= link_to post.favorites.count,  post_favorites_path(post.id), method: :post %>
                </td>
         <% else %>
            <td>
              <span> <span>いいね</span>
              <%= post.favorites.count %>
/ ---------------------------------------------------------------------------------
:javascript
  $('#star-rate').raty({
    size: 36,
    starOff:  '#{asset_path('star-off.png')}',
    starOn : '#{asset_path('star-on.png')}',
    starHalf: '#{asset_path('star-half.png')}',
    half: true,
    readOnly: true,
    score: '<haml_silent></haml_silent>'
  });
:javascript
  $('#star2-rate').raty({
    size: 36,
    starOff:  '#{asset_path('star-off.png')}',
    starOn : '#{asset_path('star-on.png')}',
    starHalf: '#{asset_path('star-half.png')}',
    half: true,
    readOnly: true,
    score: '#{@post_gouremet}'
  });
:javascript
  $('#star3-rate').raty({
    size: 36,
    starOff:  '#{asset_path('star-off.png')}',
    starOn : '#{asset_path('star-on.png')}',
    starHalf: '#{asset_path('star-half.png')}',
    half: true,
    readOnly: true,
    score: '<haml_silent></haml_silent>',
  });
:javascript
  $('#star4-rate').raty({
    size: 36,
    starOff:  '#{asset_path('star-off.png')}',
    starOn : '#{asset_path('star-on.png')}',
    starHalf: '#{asset_path('star-half.png')}',
    half: true,
    readOnly: true,
    score: '<haml_silent></haml_silent>',
  });
:javascript
  $('#star5-rate').raty({
    size: 36,
    starOff:  '#{asset_path('star-off.png')}',
    starOn : '#{asset_path('star-on.png')}',
    starHalf: '#{asset_path('star-half.png')}',
    half: true,
    readOnly: true,
    score: '<haml_silent></haml_silent>',
  });

